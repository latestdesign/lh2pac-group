"""Problem 2 — Sensitivity analysis (Sobol' indices).

Skeleton to be completed by the Problem-2 contributor. Using the ``f̂(u)`` surrogate,
compute first-order and total Sobol' indices to attribute each output's variance to
the uncertain parameters, and plot them (e.g. ``SobolAnalysis.plot``). Note the
``u``-independent outputs (``span``, ``length``) have zero variance and must be
excluded. To be done for both use cases (UC1, UC2).
"""
