"""Problem 1 — Deterministic surrogate ``f̂(x)`` and its validation.

Skeleton to be completed by the Problem-1 contributor. The surrogate-building and
validation step currently lives inside the draft in ``p1_doe.py``; it is to be
moved here: sample the true model over the 4-parameter design space with the
uncertainties frozen at nominal, fit Linear vs RBF, and assess accuracy on a
held-out test set and by cross-validation — for both use cases (UC1, UC2).
"""
