"""Problem 1 — Deterministic optimization on the surrogate ``f̂(x)``.

Skeleton to be completed by the Problem-1 contributor. The optimization step
currently lives inside the draft in ``p1_doe.py``; it is to be moved here:
minimize the maximum take-off mass ``mtom`` under the six operational constraints
on the validated surrogate, for both use cases (UC1, UC2). Its result — the
deterministic optimum — is the baseline that Problem 3 compares its robust optimum
against.
"""
